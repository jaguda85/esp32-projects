---
import Layout from '../../layouts/Layout.astro';

const getAssetPath = (path: string) => {
  const base = import.meta.env.BASE_URL || '/';
  const baseWithSlash = base.endsWith('/') ? base : `${base}/`;
  return `${baseWithSlash}${path}`;
};

const socialLinks = [
  {
    name: 'GitHub',
    url: 'https://github.com/jaguda85',
    icon: '🐙',
    description: 'Moje repozytoria i projekty open-source',
    color: 'var(--primary)'
  },
  {
    name: 'YouTube',
    url: 'http://www.youtube.com/@JagoDam859',
    icon: '🎥',
    description: 'Filmy i tutoriale o ESP32',
    color: '#FF0000'
  },
  {
    name: 'Wokwi',
    url: 'https://wokwi.com/makers/jaguda',
    icon: '🔌',
    description: 'Symulacje i projekty Arduino/ESP32',
    color: '#4CAF50'
  }
];
---

<Layout title="O mnie - Jaguda" description="Poznaj mnie i moje projekty" showHeader={false}>
  <div class="about-page">
    <!-- HEADER -->
    <div class="about-header">
      <img 
        src={getAssetPath('images/jaguda-logo.png')}
        alt="Jaguda" 
        class="about-avatar"
      />
      <h1 class="about-title">Jaguda</h1>
      <p class="about-subtitle">ESP32 Enthusiast | IoT Developer | Electronics Maker</p>
    </div>

    <!-- BIO -->
    <div class="about-bio">
      <h2>👋 Cześć!</h2>
      <p>
        Witaj na mojej stronie! Jestem pasjonatem elektroniki i programowania mikrokontrolerów, 
        szczególnie ESP32. Tworzę projekty IoT, eksploruję możliwości bezprzewodowej komunikacji 
        i dzielę się swoją wiedzą z innymi.
      </p>
      <p>
        Na tej stronie znajdziesz moje projekty, tutoriale wideo oraz przydatne linki do zasobów, 
        które wykorzystuję w swojej pracy.
      </p>
    </div>

    <!-- ESP32 SHOWCASE -->
    <div class="esp32-showcase">
      <h2>🔧 Specjalizacja: ESP32</h2>
      <div class="esp32-grid">
        <div class="esp32-card">
          <img src={getAssetPath('images/esp32-pic1.png')} alt="ESP32 DevKit" />
          <h3>DevKit v1</h3>
          <p>Standardowa płytka deweloperska z 38 pinami</p>
        </div>
        <div class="esp32-card">
          <img src={getAssetPath('images/esp32-pic2.png')} alt="ESP32 Projects" />
          <h3>Projekty</h3>
          <p>Od prostych LED do zaawansowanych IoT</p>
        </div>
        <div class="esp32-card">
          <img src={getAssetPath('images/esp32-pic3.png')} alt="ESP32 Programming" />
          <h3>Programowanie</h3>
          <p>Arduino IDE, PlatformIO, MicroPython</p>
        </div>
        <div class="esp32-card">
          <img src={getAssetPath('images/esp32-pic4.png')} alt="ESP32 Features" />
          <h3>Możliwości</h3>
          <p>WiFi, Bluetooth, ADC, PWM, i wiele więcej</p>
        </div>
      </div>
    </div>

    <!-- SOCIAL LINKS -->
    <div class="social-section">
      <h2>🔗 Znajdź mnie online</h2>
      <div class="social-grid">
        {socialLinks.map((link) => (
          <a href={link.url} target="_blank" rel="noopener noreferrer" class="social-card">
            <div class="social-icon" style={`color: ${link.color}`}>
              {link.icon}
            </div>
            <h3>{link.name}</h3>
            <p>{link.description}</p>
            <div class="social-arrow">→</div>
          </a>
        ))}
      </div>
    </div>

    <!-- STATS -->
    <div class="stats-section">
      <div class="stat-card">
        <div class="stat-number">∞</div>
        <div class="stat-label">Projekty</div>
      </div>
      <div class="stat-card">
        <div class="stat-number">24/7</div>
        <div class="stat-label">Pasja</div>
      </div>
      <div class="stat-card">
        <div class="stat-number">100%</div>
        <div class="stat-label">Open Source</div>
      </div>
    </div>
  </div>
</Layout>

<style>
  .about-page {
    max-width: 900px;
    margin: 0 auto;
    padding: 40px 20px;
  }

  .about-header {
    text-align: center;
    margin-bottom: 60px;
    animation: fadeInUp 0.6s ease-out;
  }

  .about-avatar {
    width: 150px;
    height: 150px;
    border-radius: 50%;
    border: 5px solid var(--primary);
    box-shadow: 0 0 40px var(--border-glow);
    margin-bottom: 25px;
    animation: float 4s ease-in-out infinite;
  }

  .about-title {
    font-size: 3rem;
    font-weight: 900;
    background: linear-gradient(135deg, var(--primary), var(--secondary), var(--accent));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 10px;
  }

  .about-subtitle {
    font-size: 1.2rem;
    color: var(--text-light);
    font-weight: 500;
  }

  .about-bio {
    background: var(--bg-card);
    padding: 30px;
    border-radius: 15px;
    border: 1px solid var(--border);
    margin-bottom: 50px;
    animation: fadeInUp 0.7s ease-out;
  }

  .about-bio h2 {
    color: var(--primary);
    margin-bottom: 20px;
    font-size: 1.8rem;
  }

  .about-bio p {
    color: var(--text-light);
    line-height: 1.8;
    margin-bottom: 15px;
    font-size: 1.05rem;
  }

  .esp32-showcase {
    margin-bottom: 50px;
    animation: fadeInUp 0.8s ease-out;
  }

  .esp32-showcase h2 {
    text-align: center;
    color: var(--primary);
    margin-bottom: 30px;
    font-size: 1.8rem;
  }

  .esp32-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 20px;
  }

  .esp32-card {
    background: var(--bg-card);
    padding: 20px;
    border-radius: 12px;
    border: 1px solid var(--border);
    text-align: center;
    transition: all 0.3s ease;
  }

  .esp32-card:hover {
    transform: translateY(-5px);
    border-color: var(--primary);
    box-shadow: 0 10px 30px var(--border-glow);
  }

  .esp32-card img {
    width: 100%;
    height: 120px;
    object-fit: contain;
    margin-bottom: 15px;
    filter: drop-shadow(0 0 10px var(--primary));
  }

  .esp32-card h3 {
    color: var(--primary);
    margin-bottom: 10px;
    font-size: 1.1rem;
  }

  .esp32-card p {
    color: var(--text-light);
    font-size: 0.9rem;
  }

  .social-section {
    margin-bottom: 50px;
    animation: fadeInUp 0.9s ease-out;
  }

  .social-section h2 {
    text-align: center;
    color: var(--primary);
    margin-bottom: 30px;
    font-size: 1.8rem;
  }

  .social-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 25px;
  }

  .social-card {
    background: var(--bg-card);
    padding: 30px;
    border-radius: 15px;
    border: 2px solid var(--border);
    text-decoration: none;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .social-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 3px;
    background: linear-gradient(90deg, var(--primary), var(--secondary));
    transform: scaleX(0);
    transition: transform 0.3s ease;
  }

  .social-card:hover::before {
    transform: scaleX(1);
  }

  .social-card:hover {
    transform: translateY(-8px);
    border-color: var(--primary);
    box-shadow: 0 15px 40px var(--border-glow);
  }

  .social-icon {
    font-size: 3rem;
    margin-bottom: 15px;
  }

  .social-card h3 {
    color: var(--text);
    font-size: 1.3rem;
    margin-bottom: 10px;
  }

  .social-card p {
    color: var(--text-light);
    font-size: 0.95rem;
    line-height: 1.6;
  }

  .social-arrow {
    position: absolute;
    bottom: 20px;
    right: 20px;
    font-size: 1.5rem;
    color: var(--primary);
    opacity: 0;
    transform: translateX(-10px);
    transition: all 0.3s ease;
  }

  .social-card:hover .social-arrow {
    opacity: 1;
    transform: translateX(0);
  }

  .stats-section {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
    gap: 20px;
    margin-bottom: 40px;
    animation: fadeInUp 1s ease-out;
  }

  .stat-card {
    background: linear-gradient(135deg, var(--bg-card), var(--bg-hover));
    padding: 30px;
    border-radius: 15px;
    border: 1px solid var(--border);
    text-align: center;
    transition: all 0.3s ease;
  }

  .stat-card:hover {
    transform: scale(1.05);
    border-color: var(--primary);
    box-shadow: 0 10px 30px var(--border-glow);
  }

  .stat-number {
    font-size: 2.5rem;
    font-weight: 900;
    background: linear-gradient(135deg, var(--primary), var(--secondary));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 10px;
  }

  .stat-label {
    color: var(--text-light);
    font-size: 0.9rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  @media (max-width: 768px) {
    .about-title {
      font-size: 2.2rem;
    }

    .esp32-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .social-grid {
      grid-template-columns: 1fr;
    }
  }
</style>